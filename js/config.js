// ── EDIT THESE TWO VALUES BEFORE PUBLISHING ────────────────────────────────

// Your WhatsApp channel (or Telegram/YouTube) link. Shown as a banner button
// and included in every "Share via WhatsApp" message.
const CHANNEL_LINK = "https://whatsapp.com/channel/0029VbCcxSe9cDDjKMOCbb3t";
const CHANNEL_LABEL = "Join our WhatsApp Channel";

// Used to keep your download counters separate from everyone else using the
// same free counting service. Set this to something unique to your site,
// e.g. "yourusername-bpsc-tre-papers". Change it once, before you start
// sharing the site — changing it later resets all counts to zero.
const COUNTER_NAMESPACE = "yourusername-bpsc-tre-papers";

// Base URL of your published site. Optional — if left as "", the site
// auto-detects it from the browser's current address, so Copy Link and
// WhatsApp Share already produce the correct real download URL either way.
// Set it explicitly (e.g. "https://yourusername.github.io/your-repo") only
// if you want to force a specific domain, such as a custom domain.
const SITE_BASE_URL = "";
